/test-data/.nextflow
/test-data/.nextflow.log*
/test-data/reports
/test-data/work
/test-data/results
/test-data/.command.trace

# Nextflow state from running the stub harness at the repo root
/.nextflow
/.nextflow.log*

# Local multi-version Nextflow installs (tests/setup-nextflow.sh) — never committed
/.test-tools/

/docs/build
.vscode

# Claude Code runtime state (scheduled-task lock, etc.). Project-shared Claude
# config under .claude/ (settings, commands, agents) can still be committed.
/.claude/scheduled_tasks.lock

